type Brand {
	id: ID!,
	name: String,
	country: String
	currency: String
}

input AddBrandInput {
	name: String!,
	country: String!
}


extend type Query {
	brands(count: Int): [Brand]
}

extend type Mutation {
	addBrand(brand: AddBrandInput!): Brand
}
